<%= form_with(url: exam_item_std_activities_path, model: std_activity) do |form| %>
  <% if std_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(std_activity.errors.count, "error") %> prohibited this std_activity from being saved:</h2>

      <ul>
        <% std_activity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "描述" %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label "分数" %>
    <%= form.number_field :score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
